const header=document.querySelector('[data-header]'),toggle=document.querySelector('[data-menu-toggle]'),nav=document.querySelector('[data-nav]');document.querySelectorAll('[data-year]').forEach(e=>e.textContent=new Date().getFullYear());addEventListener('scroll',()=>header?.classList.toggle('scrolled',scrollY>24),{passive:true});toggle?.addEventListener('click',()=>{const o=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',o)});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>io.observe(e));const floaters=[...document.querySelectorAll('[data-float]')];let tick=false;addEventListener('scroll',()=>{if(tick)return;requestAnimationFrame(()=>{floaters.forEach(e=>e.style.translate=`0 ${scrollY*Number(e.dataset.speed||.05)}px`);tick=false});tick=true},{passive:true});document.querySelectorAll('[data-tilt]').forEach(c=>{c.addEventListener('mousemove',e=>{if(innerWidth<900)return;const r=c.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;c.style.transform=`perspective(1100px) rotateX(${-y*2.5}deg) rotateY(${x*2.5}deg)`});c.addEventListener('mouseleave',()=>c.style.transform='')});document.querySelectorAll('.magnetic').forEach(b=>{b.addEventListener('mousemove',e=>{if(innerWidth<900)return;const r=b.getBoundingClientRect();b.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.08}px,${(e.clientY-r.top-r.height/2)*.08}px)`});b.addEventListener('mouseleave',()=>b.style.transform='')});const seq=document.querySelector('[data-sequence]'),seqImg=document.querySelector('[data-sequence-image]');if(seq&&seqImg){const total=20,frames=[];for(let i=1;i<=total;i++){const im=new Image;im.src=`assets/map-sequence/frame-${String(i).padStart(2,'0')}.png`;frames.push(im)}let current=0,target=0,last=-1;const measure=()=>{const r=seq.getBoundingClientRect(),range=Math.max(1,seq.offsetHeight-innerHeight);target=Math.max(0,Math.min(1,-r.top/range))};const render=()=>{current+=(target-current)*.085;const frame=Math.min(total-1,Math.max(0,Math.round(current*(total-1))));if(frame!==last){seqImg.src=frames[frame].src;last=frame}requestAnimationFrame(render)};addEventListener('scroll',measure,{passive:true});addEventListener('resize',measure,{passive:true});measure();render()}const glow=document.querySelector('.cursor-glow');addEventListener('pointermove',e=>{if(glow){glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'}});
